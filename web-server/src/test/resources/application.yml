
server:
  context-path: /api/tipsters
  port: 19000

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tipsters
    username: postgres
    password: root
    driver: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    database-platform: org.hibernate.dialect.PostgreSQLDialect

tipsters:
  willHillFeedUrl: http://localhost:19001/

logging.level.: INFO
logging.level:
  io.tipsters: DEBUG
